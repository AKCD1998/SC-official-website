name: Apply approved Seamless production migration

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: Type APPLY_023_INCOME_AND_024_SHOPEE_ETAX to run the approved migrations
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: seamless-production-migration
  cancel-in-progress: false

jobs:
  migrate:
    name: Migrate and verify production schema
    if: ${{ inputs.confirmation == 'APPLY_023_INCOME_AND_024_SHOPEE_ETAX' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    env:
      SC_OFFICIAL_SUPABASE_DATABASE_URL: ${{ secrets.SC_OFFICIAL_SUPABASE_DATABASE_URL }}
      SEAMLESS_PRODUCTION_MIGRATION_VERIFY: 023_shopee_income_table_indexes.sql+024_shopee_etax_documents.sql
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Confirm every prerequisite migration is already applied
        run: node ./scripts/verify-seamless-production-etax-rollout.cjs preflight

      - name: Apply pending Seamless migrations
        run: npm run seamless:migrate

      - name: Verify approved migrations read-only
        run: node ./scripts/verify-seamless-production-etax-rollout.cjs verify
