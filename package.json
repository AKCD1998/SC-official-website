{
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend-react ci && npm --prefix frontend-react run build && npm --prefix backend ci",
    "start": "npm --prefix backend run start",
    "dev:frontend": "npm --prefix frontend-react run dev",
    "build:frontend": "npm --prefix frontend-react run build",
    "preview:frontend": "npm --prefix frontend-react run preview",
    "start:backend": "npm --prefix backend run start"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6"
  }
}
