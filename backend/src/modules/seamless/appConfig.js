const appConfig = {
  appName: "Seamless X PERN",
  maxUploadMb: 20,
  maxUploadBytes: 20 * 1024 * 1024,
  maxBatchFiles: 20,
  retentionHours: 12,
  formatterModes: ["individual", "summary"],
};

module.exports = { appConfig };
